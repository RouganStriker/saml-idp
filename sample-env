# Rename as .env and configure as needed

# Local Dev
HOST=http://localhost:3000
ENTITY_ID=http://localhost:3000/sso/saml2/metadata/

ACS_POST_PATH=/sso/saml2/acs/
LS_POST_PATH=/sso/saml2/ls/post/

# SP Metadata URL
# If specified, download and parse the metadata from the URL
# and override all other settings.
# Use host.docker.internal for localhost if you are using Docker 18.03+ for mac/windows
# SP_METADATA_URL=http://host.docker.internal:3000/sso/saml2/metadata/
